import HomePage from './(app)/page';

export default function Page() {
    // This component now simply renders the page from the (app) group,
    // which will be rendered within the shared AppLayout.
    return <HomePage />;
}
